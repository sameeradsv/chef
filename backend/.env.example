# Optional — enables LLM narratives, vision parsing, and chat agent
GROQ_API_KEY=gsk_...

# Optional — enables Claude-generated recipe suggestions (MealDB still works without this)
ANTHROPIC_API_KEY=sk-ant-...

# Optional — use PostgreSQL instead of SQLite (recommended for production)
# DATABASE_URL=postgresql://user:password@host:5432/chef

# Optional — comma-separated allowed origins (defaults cover localhost + GitHub Pages)
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# Optional — Shared Cortex Auth Server URL. Validates cross-app tokens (Canopy, Chef, Circuit).
# Leave blank for Chef-only auth.
# CORTEX_AUTH_URL=http://localhost:7000
# CORTEX_AUTH_URL=https://cortex-auth.onrender.com
